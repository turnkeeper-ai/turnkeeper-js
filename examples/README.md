# Synthetic safety-exchange fixtures

The public safety-exchange fixtures live under [`docs/examples`](../docs/examples). They exercise
privacy-minimized structural contracts only: no raw messages, customer identifiers, live feeds,
case-review workflow, or authorization action is included in this repository.

Use the TypeScript SDK and the claim-safe detector adapter to validate a bounded candidate before
your authenticated server sends it to an approved hosted integration.
