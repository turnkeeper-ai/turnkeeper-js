# Server-side only. Leave the key blank when running the local simulation.
TURNKEEPER_API_KEY=
TURNKEEPER_BASE_URL=https://api.turnkeeper.ai
# Use a secret manager in production. Do not commit the real binding secret.
TURNKEEPER_BINDING_SECRET=
